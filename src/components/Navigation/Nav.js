import React from "react";

const Nav = () => {
  return (
    <nav>
      <button>Home</button>
      <button>Projects</button>
      <button>About my</button>
    </nav>
  );
};

export default Nav;
